export default function Pagination({ page = 1, totalPages = 1, onPageChange, className = "" }) {
  if (totalPages <= 1) return null;
  const goToPage = (nextPage) => { if (nextPage >= 1 && nextPage <= totalPages && nextPage !== page) onPageChange?.(nextPage); };
  return <nav aria-label="Pagination" className={`flex flex-wrap items-center justify-center gap-2 ${className}`}><button type="button" onClick={() => goToPage(page - 1)} disabled={page === 1} className="min-h-10 rounded-md border border-border-strong px-3 text-14 text-text-secondary transition-theme hover:border-primary hover:text-primary disabled:opacity-50 motion-reduce:transition-none" aria-label="Go to previous page">Previous</button><div className="flex flex-wrap items-center justify-center gap-1" aria-live="polite">{Array.from({ length: totalPages }, (_, index) => index + 1).map((pageNumber) => <button key={pageNumber} type="button" onClick={() => goToPage(pageNumber)} aria-current={pageNumber === page ? "page" : undefined} className={`size-10 rounded-md text-14 font-semibold transition-theme ${pageNumber === page ? "bg-primary text-text-inverse" : "text-text-secondary hover:bg-surface-soft hover:text-primary"} motion-reduce:transition-none`}>{pageNumber}</button>)}</div><button type="button" onClick={() => goToPage(page + 1)} disabled={page === totalPages} className="min-h-10 rounded-md border border-border-strong px-3 text-14 text-text-secondary transition-theme hover:border-primary hover:text-primary disabled:opacity-50 motion-reduce:transition-none" aria-label="Go to next page">Next</button></nav>;
}
