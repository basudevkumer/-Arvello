export default function AddressCard({ address, selected = false, onSelect, onEdit, className = "" }) {
  const title = address.label || "Delivery address";
  return <article className={`grid gap-3 rounded-lg border p-4 ${selected ? "border-primary bg-surface-selected" : "border-border bg-surface"} ${className}`}><div className="flex items-start justify-between gap-4"><div><h3 className="text-label-lg">{title}</h3><p className="mt-1 text-body-sm text-text-secondary">{address.firstName} {address.lastName}<br />{address.address}<br />{address.city}{address.postalCode ? `, ${address.postalCode}` : ""}</p></div>{onEdit ? <button type="button" onClick={() => onEdit(address)} className="text-label-sm text-primary hover:text-primary-hover">Edit</button> : null}</div>{onSelect ? <button type="button" onClick={() => onSelect(address)} aria-pressed={selected} className={`min-h-10 rounded-md border px-4 text-14 font-semibold ${selected ? "border-primary text-primary" : "border-border-strong text-text-secondary"}`}>{selected ? "Selected" : "Use this address"}</button> : null}</article>;
}
