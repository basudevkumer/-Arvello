export default function CheckoutSteps({ steps = [], currentStep = 0, className = "" }) {
  return <nav aria-label="Checkout progress" className={className}><ol className="flex items-start justify-between gap-2">{steps.map((step, index) => { const complete = index < currentStep; const active = index === currentStep; return <li key={step.id || step.label} className="flex min-w-0 flex-1 items-start gap-2"><div className="grid justify-items-center gap-2"><span aria-current={active ? "step" : undefined} className={`flex size-8 items-center justify-center rounded-full text-13 font-bold ${complete || active ? "bg-primary text-text-inverse" : "border border-border-strong text-text-secondary"}`}>{complete ? "✓" : index + 1}</span><span className={`text-center text-caption ${active ? "font-semibold text-primary" : "text-text-secondary"}`}>{step.label}</span></div>{index < steps.length - 1 ? <span className={`mt-4 h-px flex-1 ${complete ? "bg-primary" : "bg-border"}`} aria-hidden="true" /> : null}</li>; })}</ol></nav>;
}
