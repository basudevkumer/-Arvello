export default function FilterSidebar({ filters = [], selected = {}, onChange, onClear, className = "" }) {
  return <aside className={`grid content-start gap-6 ${className}`} aria-label="Product filters"><div className="flex items-center justify-between gap-4"><h2 className="text-h4">Filters</h2>{onClear ? <button type="button" onClick={onClear} className="text-label-sm text-primary hover:text-primary-hover">Clear all</button> : null}</div>{filters.map((filter) => <fieldset key={filter.id} className="grid gap-3 border-b border-border pb-5"><legend className="text-label-md">{filter.label}</legend><div className="grid gap-3">{filter.options.map((option) => { const values = selected[filter.id] || []; const checked = values.includes(option.value); return <label key={option.value} className="flex cursor-pointer items-center gap-3 text-body-sm text-text-secondary"><input type="checkbox" checked={checked} onChange={() => onChange?.(filter.id, option.value, !checked)} className="size-4 accent-primary" />{option.label}{option.count !== undefined ? <span className="text-caption text-text-muted">({option.count})</span> : null}</label>; })}</div></fieldset>)}</aside>;
}
